package org.incava.mmonkeys

class Monkeys(val count: Int) {
    fun run() {
        val toMatch = "abc"
//        val monkeys = (1..count).map { Monkey(it) }
//        println("monkeys: $monkeys")
//        monkeys.forEach { it.run(toMatch, 20) }
    }
}